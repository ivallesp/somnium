# Toy Example

Synthetic dataset with 10,000 samples and 5 correlated features (weight, height, BMI, fat, random). Trains both hexagonal and rectangular lattice SOMs to demonstrate basic usage, component plots, BMU hit maps, and U-matrix visualization.

## Run

```bash
uv run python examples/toy/run.py
```
